DIRECTORY
	iRODS/server	- iRODS servers

DESCRIPTION
	This directory, and its subdirectories, contains the components
	of the iRODS servers.  These servers provide the client interface
	(rods server), the agent that services client needs (agent), and
	the rule engine (re server).
